{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "DropAgent: start.ps1",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/start.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}
